# Testcontainers configuration for Docker Desktop on Windows

# Docker host configuration
# Testcontainers will auto-detect Docker Desktop, but we can help it
docker.host=npipe:////./pipe/docker_engine

# Enable Ryuk container for resource cleanup
testcontainers.reuse.enable=false
ryuk.container.privileged=true

# Reduce startup checks
checks.disable=false
