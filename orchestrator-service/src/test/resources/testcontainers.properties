# Testcontainers configuration for Docker Desktop on Windows

# Let Testcontainers auto-detect Docker Desktop
# Docker Desktop exposes the engine on: npipe:////./pipe/docker_engine
# Testcontainers will automatically detect this

# Enable Ryuk container for resource cleanup
testcontainers.reuse.enable=false

# Reduce startup checks for faster test execution
checks.disable=false
